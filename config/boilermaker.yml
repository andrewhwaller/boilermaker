---
default:
  app:
    name: Boilermaker
    version: 1.0.0
    support_email: support@example.com
  features:
    user_registration: true
    personal_accounts: false
  security:
    require_two_factor_authentication: false
  ui:
    typography:
      uppercase: true
      size: base
development:
  app:
    name: Boilermaker
    version: 1.0.0
    support_email: support@example.com
    description: A modern Rails application
  features:
    user_registration: true
    personal_accounts: true
  security:
    require_two_factor_authentication: false
  ui:
    theme:
      name: terminal
    navigation:
      layout_mode: navbar
    typography:
      font: CommitMono
      uppercase: true
      size: base
test:
  security:
    require_two_factor_authentication: false
