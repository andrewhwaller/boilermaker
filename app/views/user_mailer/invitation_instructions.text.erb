You've been invited!

<%= @inviter&.email || "Someone" %> has invited you to join their account.

<% if @message.present? %>
Message: <%= @message %>

<% end %>
Accept invitation:
<%= identity_invitation_acceptance_url(sid: @signed_id) %>

This link will expire in 7 days. If you didn't expect this invitation, you can safely ignore this email. 
